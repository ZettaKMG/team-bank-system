<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.klk.bank.mapper.ProductMapper">

	<select id="selectProductAll" resultType="com.klk.bank.domain.ProductDto">
		SELECT p.id,
			   p.item_name,
			   p.summary,
			   p.sav_method,
			   p.rate,
			   p.exp_period,
			   p.date<!-- ,
			   COUNT(DISTINCT r.id) numOfReply -->		
		FROM Product p <!-- LEFT JOIN Reply r ON p.id = r.product_id -->		
		
		<!-- <choose>
		<when test="keyword != '%%'">
			WHERE p.item_name LIKE #{keyword }			
		</when>
		<when test="sav_method != '%%'">
			WHERE p.sav_method LIKE #{sav_method}
		</when>
		<when test="exp_period != '%%'">
			WHERE p.exp_period LIKE #{exp_period}
		</when>
		<when test="rate != '%%'">			
			WHERE p.rate LIKE #{rate}			
		</when>
		<when test="rate != '%%'">	
			<![CDATA[
			WHERE p.rate LIKE #{rate} <= 0.02 OR
				  p.rate LIKE #{rate} > 0.02		
			]]>	
		</when>
		</choose> -->
			
		GROUP BY p.id
		ORDER BY p.id DESC
	</select>	
	
	<insert id="insertProduct" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
		INSERT INTO Product (item_name, summary, sav_method, exp_period, rate, detail)
		VALUES (#{item_name}, #{summary}, #{sav_method}, #{exp_period}, #{rate}, #{detail})
	</insert>
	
	<select id="selectProductById" resultType="com.klk.bank.domain.ProductDto">
		SELECT p.id,
			   p.item_name,
			   p.summary,
			   p.sav_method,
			   p.rate,
			   p.exp_period,
			   p.detail,			  
			   p.date<!-- ,
			   COUNT(r.id) numOfReply -->
		FROM Product p <!-- LEFT JOIN Reply r ON p.id = r.product_id -->
		WHERE p.id = #{id}
	</select>

	<update id="updateProduct">
		UPDATE Product
		SET item_name = #{item_name},
			  summary = #{summary},
		   sav_method = #{sav_method},
		   exp_period = #{exp_period},
		   		 rate = #{rate},
			   detail = #{detail}
		WHERE id = #{id}
	</update>
	
	<delete id="deleteProduct">
		DELETE FROM Product
		WHERE id = #{id}
	</delete>
	
</mapper>