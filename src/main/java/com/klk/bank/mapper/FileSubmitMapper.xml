<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.klk.bank.mapper.FileSubmitMapper">

	<select id="selectAccountByAccountNum">
		SELECT a.account_num,
			   a.account_user_id, 
			   a.account_item_id, 
			   a.account_pw, 
			   a.account_balance, 
			   a.account_date, 
			   u.user_id account_user_name 

		FROM Account a JOIN User u ON a.account_user_id = u.user_id
		WHERE account_num = #{account_num}
	</select>
	
	<insert id="insertFile">
		INSERT INTO File (file_account_num, file_name)
		VALUES (#{file_account_num}, #{file_name})
	</insert>
	
	<delete id="deleteFileByAccountNum">
		DELETE FROM File
		WHERE file_account_num = #{file_account_num}
	</delete>
	
	<select id="selectFileNameByAccountNum" resultType="string">
		SELECT file_name
		FROM File
		WHERE file_account_num = #{file_account_num}
	</select>
	
	<delete id="deleteFileByAccountNumAndFileName">
		DELETE FROM File
		WHERE file_account_num = #{file_account_num}
		  AND file_name = #{file_name}
	</delete>
		
</mapper>